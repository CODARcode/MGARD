# Build and run a simple example of MGARD-GPU

First, build and install MGARD.
Then, run ```build_script.sh``` in `examples/mgard-x/SimpleExample`.
If build successfully, run ```build/SimpleExample``` 

